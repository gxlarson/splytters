version: 2
updates:
  # Keep GitHub Actions pins current. Python deps are intentionally excluded:
  # the library declares loose ranges and the CI canary job already tests
  # against latest dependency releases.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    groups:
      actions:
        patterns:
          - "*"
